/*
 | Laravel's scaffolded bootstrap file.
 |
 | It used to import axios and hang it on window. Nothing in this site ever
 | called it — the only lines were the import and a default header — but it was
 | still bundled into app.js, where it accounted for most of the file and pulled
 | in ~20 Dependabot advisories on its own.
 |
 | Left in place rather than deleted so the conventional entry point exists if
 | something needs it later.
 */
